你在做一次剧本 skill 的验收测试。你扮演一个刚被用户用 /film-creative 调用的会话，严格按 skill 执行下面的任务。

1. 加载 skill：读 {SKILL}/SKILL.md，把它当作已被调用的 skill 指令执行；它让你读的 references / templates / scripts 一律从 {SKILL}/ 下读取和运行。不要使用 Skill 工具；不要读 ~/.claude/skills/film-creative 或 ~/.codex/skills 下的任何文件（那是别的版本）。
2. 任务：读 {TASK}，其中"用户原话"就是用户这次对你说的话；"可读材料"是你可以打开的项目文件（只读）；"禁读"列出的文件绝对不要打开，也不要用 grep/ls 去看它们的内容。需要了解项目时只用可读材料。
3. 保存：用户要求把剧本页保存到 {OUT}/{TID}.md（这就是本次的保存指令；不要写到项目目录）。剧本页按 skill 的模板写，正文必须放在 <!-- script-body:start --> 与 <!-- script-body:end --> 之间。skill 要求你跑的脚本照跑（用 {SKILL}/scripts/ 下的），临时文件只写在 {OUT}/work-{TID}/ 下。除 {OUT}/ 外不得创建或修改任何文件。
4. 自主度：用户说了"你定，直接写，不用问我"。skill 里需要停下来问用户的地方，由你自己定并在交付里写一句；skill 标为可选、需由用户决定是否执行的步骤（例如需要另开子代理的可选评审），不要执行，只按 skill 的要求在交付里提醒。
5. 最后回复：保存的文件路径 + 三行以内的说明（你按 skill 做了哪些步骤）。不要把整页贴回来。
