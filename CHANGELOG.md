# Changelog

## 3.7.0 — 2026-09-23

**本场赌注卡与场面轨合成一张事件轨：每行是一次变化，地点只是属性；"画面推进"不能再靠加地点满足。** 触发：THE ORDER EP03 场 4。用户看 v4 分镜问"整个场 4 都是球场跑步吗？"，主会话把它当成"要换地点"，给出"过线后换到休息区或牛棚"的选项；采用的 v4.1 新增"几分钟后，牛棚边"（Isa 在围栏外拉肩，Diego 绑护腿、蹲到本垒板后拍两下手套看他，Beckett 按一下 Isa 的肩走开，Isa 还坐着，全段无台词），film-director 排成 16 s 四镜整条 clip（s04-clip04）。用户看成片问："这 16 s 存在的意义是？为什么会出现这种没有意义的镜头？"

核查（不是只信主会话的根因）：
- 3.5.0 赌注卡只管"说 ≥3 句的人"，Diego 一句台词都没有，不在检查范围；v4.1 的卡把"Diego 在牛棚里蹲着等他，他还坐着"直接接在 Isa 场末"推迟"后面——画面塞进了场末栏，状态和冲刺后一样。
- 3.6.0 `check_picture` 只数"地点 × 活动"组合与时间跳，"观众这段新看见什么"一列不参与判断：v4（行行有事）报问题，v4.1（加一段没有事件的新地点）通过。
- 比主会话说的更进一步：v4.1 已被写成正确示范——测试把它当"通过"的对照样本，§3c.0c 用它作示例，列举的"新处境"第一项就是"谁在等"，§3c.8 的处理是"先试让画面推进一次"并以 v4.1 收尾，偏好账本把它记成"新地点带来新处境"的采用稿。规则不只是漏检，而是在教这样写。
- 路由层：快速路由与 §3c.7 把"都在一个地方"直接框成"换不换景"，改法选项里没有"缺什么"的诊断；删除测试只在取向 5 的整场一级。

用户在三种结构改法（A：场面轨加变化列 + 卡扩范围；B：合成一张事件轨；C：只加删掉损失列）中选 **B**，v4 类场景的判法交由 B 定——定为通过并列复核。

- `templates/script-scene.md`：`## 本场赌注` 与 `## 场面轨` 合为 `## 事件轨`。表上方人物清单（持续赌注（出处）/ 此刻向谁要 / 怕 / 为什么是现在），范围 = 说 ≥3 句的人 + 任何一行变化的主体（含不说话的人）；表里每行：谁 → 对谁、变化：进 → 出（类别：得到 / 没得到 / 推迟 / 失去 / 信息 / 关系 / 决定 / 物件 / 地位）、说出口或不说的理由与代价、删掉损失（必填）、地点（子空间）/ 活动 / 时间 / 锚句 / 估时。没有变化的行只能写"无（余韵：理由）"，且不能换地点、不能跳时间。登记例外："本场不说出口：理由""本场静止：理由"（3.6 的"本场单一画面：理由"仍认）。修订账本"场面轨重核"改为"事件轨重核"。
- `references/stage-3c-script.md`：§3c.0b 改为事件轨（地点承载变化、不代替变化；余韵只在原地；全场一个地点不是问题；脚本能查什么、不能查什么）；§3c.0c 改为"画面单调先诊断缺什么"——缺事件 / 缺人 / 缺信息 / 都不缺（交分镜），再给改法，加段或换景的选项要写出新行；3c.1 "位置"改为"地位"并与行的类别对齐；3c.2 / 3c.3 / 3c.5 / 3c.6a / 3c.7 同步；3c.8 改写"只写了地点"一行（不因一个地点换景），新增"为了画面不一样加没有事件的段"一行（v4.1 牛棚）。撤掉把 v4.1 当示范的句子。
- `SKILL.md`：取向 5 补场内每段过删除测试；取向 6 / 8 改指事件轨，取向 8 写明"推进指变化，换了地点而没有变化不算"；任务表 review 行、S3c 行、剧本层分界、交接契约（事件轨告知不锁定、余韵行不单独扩成 clip）、快速路由"整场都在一个地方"改为先诊断。
- `references/execution-contract.md` §修改请求：加段 / 换景类改法选项必须写出新行（谁：进 → 出、删掉损失），写不出的不列；批评指向画面时先交一句诊断；"删掉 / 压短""剧本不动、交分镜"都是合法选项。
- `scripts/review_script.py`：`check_stakes` + `check_picture` → `check_events`；`checks.stakes` / `checks.picture` → `checks.events`。问题：缺事件轨（旧格式另注明）、人物清单缺主要说话人或变化主体（含不说话的人）、清单上的人没写此刻要什么、锚句 / 说出口不逐字或顺序不符或不是本人说、进出相同、同一人"出"重演、**换了地点或跳了时间却标余韵或没有变化**、删掉损失空、≥30 s 少于 2 次变化未登记、清单上的人哪一行都没说出口也没写不说的理由与代价、全场无人说出口未登记。复核：换地点 / 跳时间 / 无台词且 ≥8 s / 只改变观众所知的行附变化、删掉损失、同一人上一次状态、同一对象上一行，交模型做删除测试；全场一个地点、一种活动（先诊断缺什么，不为换景加段）；余韵合计 ≥10 s；观众等一次变化 ≥40 s；自报估时偏小；分镜超 20% 提醒。阈值新增 `min_changes` 2、`gap_review_s` 40（替 `segment_review_s`）、`linger_review_s` 10、`silent_review_s` 8，删 `min_voiced`，均 `[推论]`。
- `references/dialogue-review-sources.md`：检查表合为"事件轨"一行；S6 补 Mamet 备忘录 "IS EITHER SUPERFLUOUS, OR INCORRECTLY WRITTEN."（2026-09-23 核对 No Film School 转载页；原话管整场，挪到每一行是 `[推论]`）；S13 注明 August 的例子是"同一段对白换地点演出来不一样"，不是"换地点就多一段戏"；§三 改为事件轨四样本校准表与已知边界。
- `references/preference-ledger.md`：新增"不要为换景加没有事件的段落"（用户否决 + 选 B，带适用范围与反例：余韵可留、一镜到底 / 对峙可登记、换景承载变化即成立）；3.6 画面推进一条补"同日后续修正"，作废"新地点带来新处境"的采用判断；赌注一条注明并入人物清单。
- `references/character-scene-development.md`、`scene-parameters.md`、`screenwriting-methods.md`、`screenwriting-traditions.md`、`story-engine.md`：指向改为事件轨，措辞不另改。
- 测试：`tests/fixtures/review/` 新增 `theorder-ep03-s04-v3-events.md`、`-v4-events.md`、`-v4.1-events.md`、`-v4.1-cut-events.md`（v4.1 删牛棚对照），删 `-v3-card.md`、`-v3-track.md`、`-v4-track.md`（内容已转写进 events 版）；`-v4.1-track.md` 留作旧格式样本。`test_review_script.py` 的 StakesTests / PictureTests 改写为 EventStakesTests / EventProgressTests / DocsWiringTests：v4.1 牛棚行报问题、v4 与删牛棚对照通过并列"全场一个地点"复核、牛棚改写成重演 Isa 状态报问题、Diego 不在清单报问题、Diego 写进清单并写出变化则放行但删除测试复核带证据（已知边界）、余韵只在原地、变化太少须登记（新旧两种登记写法）、锚句逐字与顺序、长等待复核、旧格式报缺、估时与提醒不变。全仓 103 项通过。

**已知未处理**：事件轨由作者自填，写得出一个变化的行脚本一律放行——"这个变化是不是前面已经给过"只能由模型按删除测试复核；四份校准样本的事件轨由维护者转写，非盲。`Last one. To the line.` 的"潜台词无支点"误报（3.4.0 起）未改。film-director 未改：其 `scripts/scene_track.py` 只认 `## 场面轨` 标题，新剧本页会回退到"无场面轨"（场地清单为空、剧本估时取不到），需在该仓库把标题正则扩到 `事件轨`（列名 地点 / 活动 / 时间 / 锚句 / 估时 仍兼容）；放大无台词行、余韵行前的检查也在那边（见本次汇报）。film-creative 仓库没有保护区哈希文件，本次只同步 VERSION / CHANGELOG。

**验收**：工程完成（测试通过）。效果提升未验证——只有 v4 / v4.1 / 删牛棚对照的非盲回填；要等用新流程写出的场与旧稿对照、最好盲评，才能说。

## 3.6.1 — 2026-09-23

**场面轨对生产侧是告知，不是锁定。** 用户看 3.6.0 的交接建议后定："我不想锁定，只需要告知即可，包含哪些场地/场景"；改法选 A"告知＋差异说明"。

- `SKILL.md` 交接契约：场面轨告诉 film-director 本场有哪些场地 / 场景、每段做什么、大致多长；分段、子空间、活动乃至地点都可由生产侧改，在分镜方案里写一行与场面轨的差异和理由。原有"已确认的台词与场景是锁定输入"改为只锁台词，场地与场景安排为告知；生产侧改动要不要回写剧本页由用户决定。3.6.0 的"回流条件"改为"时长提醒"：分镜超出场面轨总估时 20% 以上时在方案里标出、提醒用户，不停下、不自动退回。
- `references/stage-3c-script.md` §3c.0c 同步一句；`scripts/review_script.py` `--production-total` 的输出从"回剧本层重核"改为"提醒，是否回剧本层由用户定"（不计为问题，阈值不变）；`dialogue-review-sources.md`、`preference-ledger.md`、README 同步措辞。
- 测试：回流测试改为"提醒不是问题"，文档测试断言"告知，不锁定"且不再有"台词与场景是锁定输入"。99 项通过。

film-director 仍未改（另一仓库）；交接建议相应改为：把场面轨当参考，改场地或分段时在方案里写差异，超 20% 标出提醒。

**验收**：工程完成（测试通过）；本次只改交接约束力，不涉及效果判断。

## 3.6.0 — 2026-09-23

**画面推进成为剧本层的产物：写正文前填"场面轨"，review 另报"画面"，交接加回流条件。** 触发：THE ORDER EP03 场 4 v3 与 v4 整场都在"Sharks 球场外圈跑道"上跑步（并排跑着说 → 冲刺 → 过线）；剧本估 57 s，film-director 按内容排出 74 s，只能靠侧跟拍 / 反向跟移 / 身后跟拍 / 手持斜前四种机位轮换补变化。用户看分镜时才问"整个场 4 都是球场跑步吗？"，定 v4.1 过线后"几分钟后，牛棚边"，并问"film-creative 再出剧本的时候是不是不会说明场地/画面"。

**核查结论**（场地其实写了：场景标题、场景注释、空间一节都有；缺的是"这场几十秒里观众看见的东西有没有变"）：
- 边界句把画面整体推给了生产层：`SKILL.md`"镜头与 clip 属于生产层，本 skill 不推导"没有区分"用什么镜头看"与"观众在哪里看见什么"；§3c.3"不强制走位……不把剧本开发变成分镜设计"；模板"空间"栏"起止位置（持续不动也可）"把静止当默认。
- 写正文前唯一的必需产物是赌注卡（人物维度）；地点在 S3b 场景表里只占"地点 / 时刻"一格，进了场景标题就没有再被问过。一手来源里有这一步却没接进来：`screenwriting-traditions.md` 早已引 John August《How to write a scene》，但只取了进入方式与脑内排演，第 4 步"Where could the scene take place?"与第 6 步"长场还是短场"没有进流程；H1 还写着"不只列地点"。
- 取向 8"同一地点仍有新作用时不必换景"与 `story-engine.md` §二是跨场规则，没有场内版本，在场内被读成"不换也行"。
- 改稿范围规则放大了问题：v4 按"只改被批评的部分"原样保留 v3 的晨跑场景，只换台词——61 词 → 142 词、估时 47 → 57 s，画面始终是跑道上跑步；内容变了之后没有一步重核"旧场景装不装得下"。
- 估时偏小：节拍表是"需要时正文后补"，估时约等于台词时长，无台词动作（越过、冲刺、过线、看板、Cole 一眼）没有下限；场 4 冲刺 + 过线两段剧本估 14 s，分镜排 24 s。
- 出稿后默认 review 只看对白；交接只交剧本 + 赌注卡 + 参数卡 + ip，没有"画面怎样推进"；film-director 把场景当锁定输入、只能改机位，估时超出也没有回流触发——问题到用户看分镜才暴露。

**改动**：
- `templates/script-scene.md`：赌注卡之后、剧本页之前新增"## 场面轨"（段 / 地点（子空间）/ 主要活动（类别；变化写括号）/ 时间 / 谁在画面里 / 观众这段新看见什么 / 锚句（逐字）/ 估时）+ "本场单一画面：理由" + 总估时；修订账本加"场面轨重核"栏；"空间"栏去掉"持续不动也可"；节拍表不再另估总时长。
- `references/stage-3c-script.md`：新增 §3c.0c 场面轨（剧本层与生产层的分界、先问地点与长短、分段规则、判断、估时方法、与赌注卡的关系）；3c.1 观众侧、3c.2、3c.3、§对白审阅（三项结论）、3c.6a 连读、3c.7 重写（内容或估时变 ≥20% 重做场面轨，换不换景按改法选项处理）、3c.8 新增"场景只写了地点"一行。
- `SKILL.md`：取向 8 补场内版本；边界句改为"画面推进属剧本层，镜头与 clip 属生产层"；S3c 流水线行、review 任务行、交接契约（场面轨随剧本交付 + 回流条件：分镜实排比场面轨总估时多 20% 以上回本 skill）、快速路由一条。`screenwriting-methods.md` H1 接回 August 第 4、6 步并加失效信号"轮换背景"；`screenwriting-traditions.md` §五、`story-engine.md` §二（本行管跨场）、`scene-parameters.md`（画面推进不进参数卡）各一处指向。
- `scripts/review_script.py`：新增 `track_card()` / `timeline()` / `check_picture()`——锚句逐字在正文动作行里且顺序一致；按括号外的"地点 × 活动"数组合、按"时间"栏数时间跳；估时 ≥30 s 缺轨判问题（只在总判断里报一句，不占 ≤3 个问题的位置）；全场一个组合、无时间跳、未登记理由判"画面没有推进"；一段画面文本估时 ≥40 s、自报总估时 < 文本估时 × 0.85 列复核；`--production-total` 超剧本估时 20% 在总判断里提示回剧本层。文本估时 = 台词词数 ÷ 语速（`--wps`，默认 4）+ 无台词段动作句 × `--action-sec`（默认 1.5 s；台词之间 ≤2 句的反应插入不计）。结论 `checks.picture`；脚本不判地点好不好、活动够不够丰富，语义交模型复核。
- `references/dialogue-review-sources.md`：新增 S13（John August 2007，2026-09-23 重读，第 4、6、8 步核对）；§二 画面推进行；§三 估时 5 场校准表与画面组合 3 样本表；§四 复核记录。
- `references/preference-ledger.md`：新增 2026-09-23 画面推进条（适用范围与反例：余韵戏、一镜到底、困住 / 对峙的压迫感可登记单一画面；同一地点处境在变不算问题；换景要带新处境；已认可的画面不删；不写镜头）。
- 测试：`tests/fixtures/review/` 新增 EP03 场 4 三份——v3（原稿 + 回填赌注卡与场面轨）、v4（重建：v4 原文未单独保存，正文取 v4.1 采用稿跑道部分 + 分镜卡里的"Isa 看板"，去掉牛棚段）、v4.1（采用稿 14:21 快照 + 回填场面轨）；`test_review_script.py` +10：v3 / v4 判"画面没有推进"、v4.1 通过（2 组合 1 次时间跳）；文本估时落在剧本自报与分镜实排之间、误差 ≤16%；`--production-total` 88 / 74 触发回流、80 不触发；缺轨只对 ≥30 s 的场；单一画面登记理由后不报；锚句不逐字 / 顺序颠倒报问题；长段画面列复核；`--wps` / `--action-sec` 生效；文档接线。旧测试三处按新行为更新（EP02 旧样本另报缺场面轨；≤3 个问题不计只在总判断里报的缺轨；合成正例 `picture = n/a`）。

**校准**（全部 `[推论]`）：文本估时对已出分镜的 5 场误差 0.85–1.15（EP02 场 1、EP03 场 1–4），只够作复核提示；剧本自报 / 分镜实排只有场 4 两版低于 0.8（0.78 / 0.77），其余 0.88–0.98——回流阈值 20% 据此定。只按地点判会把 5 场里的 4 场报出来（标题都是一个地点），所以按"地点 × 主要活动"判。

**已知未处理**：v4 / v4.1 的 Isa「Last one. To the line.」被判"潜台词无支点"属误报——白线写在前面的中文动作行里，脚本对不上英文指称（3.4.0 起的已知限制）；本次不改。场面轨的活动类别由作者自填，如实与否交模型复核；v3 / v4 / v4.1 的轨由维护者回填，非盲。film-director 未改：它把场面轨当锁定输入、按回流条件退回，需在该仓库另行配合（交接建议见本次汇报）。film-creative 仓库没有保护区哈希文件，本次只同步 VERSION / CHANGELOG。

**验收**：工程完成（测试通过）。效果提升未验证——需要用 3.6.0 流程新写的场次，与 3.5.0 流程写的场次对照（最好盲测），看分镜实排 / 剧本估时和"整场一个画面"是否减少；现有样本只证明工具能暴露问题、能放过 v4.1。

## 3.5.0 — 2026-09-23

**人物赌注成为写台词前的必需产物，并进入交接与审阅链。** 触发：THE ORDER EP03 场 4 v3（晨跑）被否决——"没有突出人物自身的压力和希望'出道'（进名单、先发）的情绪，更多是在平铺说话"，用户指出场 1–4 都有此问题。3.4.0 的 review 对场 1–3 判"通过"。

**核查结论（以 EP03 场 1–4 为证据）**：
- 写台词前唯一必需的产物是承接清单：`stage-3c-script.md` 3c.0 只要求提取"进场状态、知识边界、后文仍须成立"；3c.1 的"谁要什么"写着"缺表不补表"，模板里放在正文后、标"按需后补"。场 4"后文仍须成立"三条（Diego 说出摇头的事、Cole 替 Diego 说话、周一 CUT DAY）正好是全场台词内容。
- "谁要什么"只问本场、不接人物的持续赌注；场 1 的"两侧"写了 Diego UNDER REVIEW、Isa 被记住两次摇头，台词却是流程问答。`scene-parameters.md` 只有场景级参数。
- 约束只有禁止项：取向 6 与 §五 的"不替人物总结主题 / 不解释 / 克制"可检查，"直说有资格"只是许可，叠起来的安全解是人物不表态。偏好账本 2026-09-08"承上启下先于机灵"只排了承接的优先级。
- review 工具只衡量连通与锚定，"通过"被项目当成验收；探针：把场 4 "That's fair." 换成 Isa「Not my scholarship.」，3.4.0 判"潜台词无支点"并建议"直说来历 / 第三人问"——人物说出自己的赌注反被罚，回指前场的台词永远安全。
- 执行者一侧（项目层，本次不改）：两侧栏在正文后补写、对白审阅栏夸的是回收功能；没有读 ip.md 的人物动机；项目交付标准"补测状态只在框架规定位置出现一次"与"棒球相关 ≤20 s"压缩了说出赌注的空间。

**改动**：
- `templates/script-scene.md`：剧本页之前新增"## 本场赌注"卡（人物 / 持续赌注（出处）/ 此刻向谁要什么 / 怕失去什么 / 为什么是现在 / 说出口（逐字）或不说的理由与代价 / 场末）；"上下文承接"注明是约束不是台词清单；"两侧"人物侧指向卡；"对白审阅"先对卡。
- `references/stage-3c-script.md`：3c.0 拆为 3c.0a 故事依据（约束）与 3c.0b 本场赌注（必需产物）；写明优先级——台词先分给卡上的欲望，承接进动作行或夹进欲望句；全场不说出口须登记例外；§对白审阅说明两项结论；§3c.8 加"台词都在处理剧情账"一行。
- `references/character-scene-development.md` §五：约束管"怎么说"不管"说不说"；§七 加误用信号。`references/scene-parameters.md`：人物级内容不进参数卡。`SKILL.md`：取向 6 补一句、S3c 流水线行、review 任务行、交接契约含赌注卡。
- `scripts/review_script.py`：新增 `stakes_card()` / `check_stakes()`——逐字核对卡上说出口的句子是否在正文、是否本人说、下一句是否接；不说的是否写了理由与代价；本场说 ≥3 句的人是否上卡；全场是否至少一人说出口（可在卡下登记"本场不说出口：理由"）；缺卡即报问题。脚本不判一句话"有没有欲望"，"那句说的是不是卡上那件事"一律列为需模型复核。结论分 `checks.dialogue` / `checks.stakes`，终端首句分开报；赌注问题排第一；卡上声明的赌注词不再报"潜台词无支点"；画外句只并入"没有形成来回"一条；超 800 字时最后一步去掉问题的"为什么"。
- `references/dialogue-review-sources.md`：S6 Mamet 备忘录 2026-09-23 重读逐字核对，由复核框架升为赌注卡结构依据；新增 S12 Scriptnotes 403（Mazin "Fear is our connection to a character"）；§二 新增人物赌注行、§三 新增 EP03 场 4 校准表、§四 复核记录。
- `references/preference-ledger.md`：新增 2026-09-23 条（适用范围与反例）；2026-09-08"承上启下先于机灵"补边界：承接是约束，不排在人物赌注之前。
- 测试：`tests/fixtures/review/` 新增 EP03 场 4 v3 原稿与"按原稿两侧栏如实回填的卡"两份（正文不改）；`test_review_script.py` +10：原稿报缺卡且排第一；回填卡报 Isa 上卡未说、Diego 兑现但无人接、Isa 推迟无代价；合成正例两项都通过；卡与正文不符、说话人不符、不说无理由、主要说话人漏卡、全场不说（有 / 无登记例外）、安静戏与独角戏不适用、探针 "Not my scholarship." 有卡不报；EP02 旧样本对白连通仍通过、赌注为缺卡。89 项通过。

**已知未处理**：场 4 "That's the job." 的 job 被判"潜台词无支点"属误报（通用名词），本次不改该检查。film-director 未改（交接契约只说明卡随剧本页交付）。

**验收**：工程完成（测试通过）。效果提升未验证——需要用新流程重写的场 4 与 v3 原稿对照（最好盲测）才能说；现有样本只证明工具能暴露问题。

## 3.4.0 — 2026-09-21

**review 工具新增"潜台词无支点"检查与改法输出。** 触发：EP02 场 3 v2 钥匙段——Beckett "Your grey hoodie's in my bag… half my stuff is still at your place." → 动作行"Cole 指了指钥匙圈上多出来的那一把" → Cole "So go get it. You have a key."；用户："这个太委婉了，没有上下文铺垫和场景铺垫，看不懂。"观众要从一个手势推出三层设定，前文没有铺垫。

- `scripts/review_script.py`：新增 `anchoring()`——逐句抽取"当作已知"的指称（限定词 / 物主 + 名词短语、"you have a X"、配 still / again / all over 的名词），对照前文（本场已出现的台词与英文动作行 + `--context` 前几场）；指称是新的、本句是省略句（≤5 词、回应词起句、截断的短句）、接下来没人问 → "潜台词无支点"，含 imperative / 决定的句子排最前；`this / these`、"right here" 视为指画面里的东西；模板头部与"上下文承接"里声明过的指称（如账本写明的旧梗）降为"需模型复核"；本句明说的新设定列为信号。问题附三种改法（直说来历 / 用当场动作引出 / 让第三人替观众问，各含改什么 / 这场变成什么 / 影响后面什么），按 `execution-contract.md` §修改请求 作为改法未定时的选项，不改稿。`basis` 同步标注：共同基础与收件人设计有来源，代理量是 `[推论]`。中文动作行无法与英文指称对齐，只引用交复核。
- `references/dialogue-review-sources.md`：新增 S10 Clark & Brennan 1991（p. 127 共同基础定义，Stanford 开放 PDF）、S11 Scriptnotes 693（凭空出现生硬 / 当场需要 / 当场问）；S1 补 p. 727 recipient design；§二、§三、§四 对应行；未读的 Clark & Marshall 1981、Levinson 1983 如实列为不引用。
- 接入：`SKILL.md` 任务表该行加 `--context` 与改法说明；`stage-3c-script.md` §对白审阅 同步。
- 测试：`tests/fixtures/review/` 新增场 3 v2 修复前（正例）、修复后（反例，用户选定改法 C 后 Mack 替观众问）、场 2 v2；`test_review_script.py` +4：正例报 key 且排第一、附三种改法；反例三份不报；"Sloane, you might want to step back." / "Nothing." / "Everything." 不在候选；账本声明的 lake house 降为复核。79 项通过。**工程完成；效果为非盲呈现改善——正反例都是校准样本。**

## 3.3.1 — 2026-09-21

只做来源复核与输出标注，不改判定逻辑与阈值。

- `references/dialogue-review-sources.md`：逐条复核九条来源（可回访 / 性质 / 本次是否已读 / 支持是否直接），新增 §四复核记录表。更正建档时"全部为本次实际读取"的说法：S7（Sciamma BAFTA 讲稿）本次访问 403，未重读，降为旁证并标 `[本次未重读]`；S6（Mamet 备忘录）明确为第三方转载页、无官方原件；S4 注明 LDC 的 utterance 切分细于说话轮，只作阈值锚点；S2 注视 → 同框、S8 调度 → 代理量、S9 视线 → 画外句判问题三处间接推论逐一标出。
- `scripts/review_script.py`：每个问题新增 `basis` 字段并写进终端输出——哪几条来源直接支撑、哪些阈值与代理量是 `[推论]`；结尾一行注明"判为通过只表示没触发已知问题"。`tests/test_review_script.py` 加断言。75 项通过。

## 3.3.0 — 2026-09-21

**出稿后的对白 review 工具。** 触发：THE ORDER EP02《夜泳》场 1 的台词连续两版被用户否决（"不像人说话，断断续续的，没有形成交流""主谓宾不是这样的，更像是写给每个角色的一句话"）。事后诊断：十句话七个说话人没有一组来回；三句画外配在别人脸上、每句一镜；全是 2–4 词短句。`dialogue-diagnostics.md` 等自查清单在出稿后没有被系统执行，判断也没有可回访的一手来源，所以没拦住。

- 新增 `scripts/review_script.py`：读剧本页正文（`**NAME**` 或独立角色名 + 台词；括号为提示），算收件人链（点名 / 回答问句 / 词汇回声 / 回应词起句 / 追问）、两人来回轮数、有人接的台词占比、连续互不接话的段落、平均词数与无理由的 ≤4 词短句占比（回答问句、喊名、齐喊、明显接住上一句豁免）、主谓宾完整度（英文启发式）、画外 / O.S. / V.O. 标注、换到第三人且不接前句的比例（一句一镜的剧本层代理量）。语义判断不冒充已判定：收件人不明、能否同框的句子输出为"需模型复核"。输出按 §默认输出契约的诊断格式（一句总判断 + ≤3 个问题，各含台词原文证据与来源编号，≤800 字），`--json` 给逐句特征与阈值；画外句同时作为给 film-director 的交接提示。不改稿、不替用户采用；零外部依赖；退出码 0 / 1 / 2。
- 新增 `references/dialogue-review-sources.md`：本次实际读取的九条一手来源（Sacks/Schegloff/Jefferson 1974、Stivers et al. 2009、Levinson & Torreira 2015、LDC Switchboard 统计、Scriptnotes 609、Mamet 备忘录、Sciamma 2019、Bordwell 2013、Smith 2012），各记可信范围与支持哪条检查；明确哪些检查有来源、哪些是本 skill 的 `[推论]`（阈值、"三轮才算来回"、4 词/s 换算、剧本层代理量）；未读到的（McKee、Grice、BBC 格式指南）如实列出不引用。
- 接入：`SKILL.md` 流水线 S3c 行（落盘后默认跑，只 review 不改稿）、任务表新增"出稿后 review 对白"行；`stage-3c-script.md` §对白审阅 写明脚本算什么、模型复核什么、按哪个格式交付；README 文件地图。未改 SKILL.md 其他内容，未改 film-director。
- 验证：`tests/fixtures/review/` 收 v1（按 clip02 v1 备份分镜卡重建）与 v3（采用稿正文）；`tests/test_review_script.py` 10 项——v1 判为"没有形成来回 + 一人一句的标语化"（最长来回 0 轮、有人接 40%、平均 3.0 词、画外 3 句），v3 通过（来回 7 轮、有人接 87%、平均 6.0 词、画外 0）；两句安静戏不判、短答有问句豁免的两人戏通过、中文剧本页解析并跳过句长指标、O.S. 与（画外）两种标注都识别、退出码与 JSON。阈值只用这两组样本校准，全部 `[推论]`。**工程完成（测试通过）；效果为非盲呈现改善，未做盲测。**

## 3.2.0 — 2026-09-20

**修改请求拆成范围、改法、采用三个独立判断。** 触发：用户对一场戏说"这里我觉得调整一下吧，攻击性太强了"，3.1.1 直接改稿并把改稿同步进故事正文、分集框架与方法文档。定位到三处规则叠加：`execution-contract.md`"新修改请求授权其范围内修订，不再问同一授权"把批评当成改法授权；"何时停"只在用户主动要选方向时停；`creative-loop.md` §二诊断后默认进入改稿。同步依赖文档则违反了已有的"探索稿不自动替换采用稿 / 模型自主选择不是确认"，诱因是项目"直接落盘"的保存约定让落盘看起来等于采用。

- `execution-contract.md` 新增 §修改请求：**范围**（批评即授权，其余硬锁保留）；**改法** given / open——open 且解法不止一种、会改变人物或后续时交付两到三种改法（改什么 / 这场变成什么 / 影响后面什么），不改稿，用户点选后再改；解法唯一或只涉措辞直接改；autonomous 时自主选一种并说明未选的；判断依据是解法空间不是触发词；**采用**——模型改稿默认探索稿，落盘 ≠ 采用，采用是用户动作；采用前不同步故事正文、分集框架、`ip.md`、速览表、方法文档，受影响处列待复核；给定改法原位改并先存快照，模型自选改法进 drafts。"何时停"加入改法选项停靠。
- `route_check.py` / `templates/execution-record.json`：新增 `request.method`（given / open / unspecified）与 `request.adoption`（exploratory / adopted）；R08 拒绝"open + guided 却交付 rewrite"和"未确认却标 adopted"；decision 输出 `rewrite_allowed`、`sync_dependents`。
- `SKILL.md`：任务表拆出"批评已有稿、改法未定"与"优化单场（改法已定或用户说你定）"两行；需求识别段、硬规则 4（改稿采用前不算数）、快速路由三条。`creative-loop.md` §二 加"先判改法，再决定改不改"分支，§三 第 1、5 条对齐；`story-context.md` §4"采用是用户的动作"；`project-state.md` drafts / current / approved 的进入条件；`stage-3c-script.md` §3c.7；`preference-ledger.md` 记 2026-09-20 反馈。
- 测试：`test_creative_boundaries.py` +3（open+guided 拒绝 rewrite、given/autonomous 允许、adopted 需确认）；`test_story_workflow.py` +1 措辞在场；65 项通过。
- 验证（`tests/acceptance-3.2.0/`）：合成项目（保存约定"直接落盘"）上四次独立运行——3.1.1 复现了直接改稿并同步依赖文档；3.2.0 候选对改法未定的批评给三种改法且零文件变更，对给定改法只改该场并列待复核，对"你直接改"自主改进 drafts 并说明未选改法。单次样本，非盲；未重跑 T1–T9（改动不涉及故事与对白方法）。

## 3.1.1 — 2026-09-19

只修诊断交付过长。3.1.0 的 T2 输出为 3,068 / 3,190 字符（约 2,200 汉字，8.1 / 8.2 KB UTF-8；此前报告误写为“8k 字”，已更正单位），原因是任务表、`stage-3c` §3c.5b 与通用输出契约三处叠加要求，输出长成“结论 / 已确认 / 信号 / 不是问题 / 总结 / 下一步”六段。

- `SKILL.md` §默认输出契约新增“诊断任务的交付”（唯一格式来源：一句总判断 + 最多三个独立问题，各含证据与影响；同一根因合并；不分栏目重复展开；默认约 500 字、最多 800 字；用户要求详细时按其要求展开；结尾只说最该先处理什么）与“创作任务的交付”（字数限制不适用于概念、故事、分集框架、剧本；过程默认不展示；简洁不等于删场景和对白）。任务表诊断行、快速路由、`stage-3c-script.md` §3c.5b 改为指向该段，删除“三到五条 + 信号单列”的重复要求。
- 更正 `tests/acceptance-3.0.0/`、`tests/acceptance-3.1.0/` 报告中的长度单位（字符数 vs UTF-8 字节数）；历史输出原文未动。
- 验证：只复跑 T2 一次（`tests/acceptance-3.1.0/T2-rerun-3.1.1.md`）：967 字符 / 712 汉字，一句总判断 + 三个问题，含证据与影响，无改稿、无重复栏目、无服务菜单。未重跑创作任务；字数约束不进入创作任务只做了规则核对。

## 3.1.0 — 2026-09-19

范围受控的误伤修正，不新增模块、模板或评分体系。基线 3.0.0（`v3.0.0`）保留为可恢复的比较版本。

- **正文具体 ≠ 禁止否定句**：硬规则 8、`story-engine.md` §七、`stage-3b-story.md`、`episode-design.md` §二、`creative-loop.md`、`preference-ledger.md` 六处改为同一口径——不进正文的是"解释自己遵守了什么规则"和"用抽象评价代替剧情"；人物拒绝、不回应、没做成的事都是戏剧行动，写出形态与后果；不按句式判断。
- **避免口号化 ≠ 人物不能表态**：`SKILL.md` 取向 6、`character-scene-development.md` §五、`stage-3c-script.md` §3c.5b——明确拒绝、承诺、坦白、划清边界都可以是有效对白；判断看面对谁、刚发生了什么、为什么此刻、说完对方怎么接；允许必要的情绪表达；保留"直给优于落不了地的比喻，但直给不是解释一切"。未追加任何禁止条款。
- **持续交集 ≠ 必须有制度理由**：`story-engine.md` §三——主动追求、共同要完成的事、承诺、责任、过去的选择同样能制造交集；判断可信度与"关系变化后相遇 / 离开的条件是否随之变化"；一直绑在同一组织里也是问题。
- **机制持续 ≠ 定期插入题材事件**：`story-engine.md` §一、`stage-3b-story.md`、`templates/story.md`——删除"每隔多久必须再碰一次""周期性公开节点"；判断改为"上一次选择的后果是否改变下一次的条件"；体育剧不需要频繁回到比赛，但竞争产生的机会、立场、信任、代价不能无故消失；"what is episode 100"限定为长线剧，有限季只问每一段是否由前一段推出。
- 验证（`tests/acceptance-3.1.0/`）：3.0.0 与冻结后的 3.1.0 候选在独立会话重跑 T1–T6，并由未参与修改的子代理出 3 道新题（连续三集完整正文；两场对白各跑两次），共 22 次运行，盲序评审：候选 3 胜 · 基线 1 胜 · 7 平。旧题无退步；T5 作为已知问题回归持平；新题收益在三集正文与表态对白第二次，不能说破的对白一平一负。匿名正文在 `materials/`，版本对应表单独存于 `materials-key.json`。**工程完成；效果为维护者非盲判断，对白项视为持平。**

## 3.0.0 — 2026-09-19

直接基于 1.1.0（2026-09-17 回滚点）。版本号跳过 1.2.0–2.1.2：这些标签属于已回滚的存档线（`archive/pre-rollback-2026-09-17-main-at-v2.1.2`），本版不继承其文件（设计表、情绪资产、handoff-contract、canon_scan 等），只恢复其中用户已确认的偏好记录。

**诊断依据**：一季故事 v1 → v2 的用户反馈（空间单一、球员魅力不足、人设不讨喜、感情幼稚无事件推动）与 v2 文本本身——分集框架写成"空间 / 事件 → 选择与变化 → 结尾"的抽象陈述，充满"没有……也没有……"，中段连续多集换景不换机制；1.1.0 没有任何文件判断"概念能否持续产生事件""两线是否互改""观众为什么在乎"，对白只有诊断规则没有正向工艺，"诊断 / 单集 / 单场 / 对白"没有各自的交付范围。

- `SKILL.md`：核心判断；十条默认创作取向（任务中用户明确要求优先）；任务模式与交付范围表；S3b / S3c 读取项接新文件；硬规则 8（正文先写会发生什么，否定句留在自检）、9（反模式是信号不是判决）；快速路由三条。
- 新增 `references/story-engine.md`（§一 概念→机制三判断 · §二 两线互改 · §三 吸引 / 交集 / 代价与确认后的张力 · §四 触发链与隐瞒三问 · §五 配角与场景 · §六 观看欲望 · §七 正面写法 · §八 误用信号）。
- 新增 `references/episode-design.md`（§一 一集四件事 · §二 分集框架不像摘要 · §三 单集分场步骤 · §四 平台公式边界 · §五 诊断）。
- `stage-3b-story.md`：正文写会发生什么；四判断之外的两项引擎检查；多集行改指向 episode-design；常见失败加四行。`stage-3c-script.md` §3c.5b 任务范围提醒。`character-scene-development.md`：隐瞒 / 误会 / 退缩三问；潜台词有动机、直说有资格（Mamet 三问、Birch）。`creative-loop.md` §二 加四行诊断。`templates/story.md` 加"机制与两线"按需段。
- `preference-ledger.md`：2026-09-19 十条取向与"正文写发生了什么"；恢复 1.3.x 的三条用户已确认偏好（直给优于比喻、承上启下先于机灵、外部机制不能消失——按 09-19 表述修正）。
- `context-creativity-sources.md` §3.0.0：Parker & Stone、Mazin 403、Gilligan 2014、Scriptnotes 478、Mamet 备忘录、Tierney × 2、Birch、August 2025 微短剧、中文短剧培训文（作为不采用的对照）、郑瑞景重读；未读到 Curtis / BBC Writersroom 并如实标注。
- 测试：`test_story_workflow.py` 加新文件在场与 SKILL 接线检查；`test_routing.py` 把新文件纳入退役门禁短语扫描。
- 验证（`tests/acceptance-3.0.0/`）：六个固定任务，新旧版各在独立会话运行，盲序评审（评审者为维护 Agent，非用户）：3.0.0 胜 3（诊断 / 单集 / 一季骨架）、1.1.0 胜 1（对白）、平 2（两个过拟合检查项）。对白项复测两轮一致偏"表态"，根因是"直说也有资格"被读成可直接表态，已改措辞并把规则里取自测试原文的示例换成中性句，复跑后与旧版持平。**工程完成；效果提升为非盲呈现改善，未经用户盲选。**

## 1.1.0 — 2026-09-07

- 新增故事上下文协议：写前读取整体/本场/远处依赖，区分故事事实、人物所知与观众所知，保护后续兑现；采用后更新变化量，跨轮恢复回读原文。
- 新增创意开发操作：在已有边界内探索人物理解、关系、选择与后果，实际写出差异，按收益与修订代价选择。
- 提炼好莱坞与韩国编剧一手资料为 H1–H3 / K1–K4 七项方法，覆盖 August、Gilroy、Mazin、奉俊昊、朴赞郁、郑瑞景、李沧东；接入故事/剧本开发，记录方法适用条件和来源。
- 接入 intake、概念、故事、剧本、重写、恢复及可选场景摘记；不强制用户填表，不新增保存或确认门槛。
- 修复路由检查器错误接受 S4–S7/performance/raw；生产阶段仍可作为排除/交接信息，执行范围与独立创意 Skill 对齐。旧错误的生产执行记录现会返回错误。
- 清理悬空生产模板，修正“本场用不到就删正典”、非因果画面一律淘汰和靠拆 clip 增加总时长的冲突规则。
- 验证：60 个 unittest 与 shell 回归通过；13 个合成案例非盲文本走查。未进行独立创意盲测或长上下文效果基准，不宣称已证明创意增益。详见 `tests/acceptance-1.1.0/review.md`。

## 1.0.0 — 2026-09-07

从 [Film-Seedance-Director](https://github.com/Anelse0/Film-Seedance-Director) 2.6.0-alpha.3（commit 0436abd）拆分出的创意前端 Skill，首个独立版本。

- 范围：S1 资源读取、S2 需求识别、S3a 概念、S3b 故事、S3c 剧本、局部改写与创意评估。
- 生产后端（S4 表演外化、S5 分镜、S5b 参考资产、S6 Prompt 编译、S7 检查）拆至独立的 film-director Skill；本 Skill 中对生产文件的引用改为交接说明。
- 移除生产侧文件：stage-4/5/5b/6/7、emotion-performance 库、capabilities、camera-vocabulary、director-lenses、externalization-lexicon、production-workflow、performance/production 记录与模板、validate_prompt 及生产脚本、生产示例与测试。
- 共用文件（execution-contract、stage-1-intake、scene-parameters、causal-chain、anti-mechanical、genre-packs、project-state、script-scene 模板）按创意侧范围裁剪；genre-packs 保持与 film-director 相同内容。
- 历史版本记录（1.2.0–2.6.0-alpha.3）见源仓库 Film-Seedance-Director 的 CHANGELOG 与 tags。
